[package]
name = "SpiFlash-for-embassy"
version = "0.1.0"
edition = "2024"

[lib]
name = "SpiFlash_for_embassy"
path = "src/lib.rs"

[features]
default = []
defmt = ["dep:defmt"]

[dependencies]
embedded-hal = "1.0.0"
embedded-hal-async = "1.0.0"
defmt = { version = "1.0.1", optional = true }
